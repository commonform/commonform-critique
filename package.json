{
  "name": "commonform-critique",
  "description": "suggest improvements to Common Forms",
  "version": "0.0.0",
  "author": "Kyle E. Mitchell <kyle@kemitchell.com> (http://kemitchell.com)",
  "bugs": "https://github.com/commonform/commonform-critique/issues",
  "devDependencies": {
    "chai": "^2.2.0",
    "check-versions": "^1.0.0",
    "istanbul": "^0.3.5",
    "jscs": "^1.8.1",
    "jshint": "^2.5.10",
    "mocha": "^2.0.1"
  },
  "homepage": "https://commonform.github.io",
  "keywords": [
    "contracts",
    "document assembly",
    "knowledge management",
    "law",
    "critique",
    "markup"
  ],
  "license": "Apache-2.0",
  "main": "source/index.js",
  "peerDependencies": {
    "commonform": "1.x"
  },
  "repository": "commonform/commonform-critique",
  "scripts": {
    "check": "check-versions --check-module",
    "coverage": "istanbul check-coverage --config .dotfiles/istanbul.yml coverage/coverage.json",
    "critique": "jshint --config .dotfiles/jshint.json source test && jscs --config .dotfiles/jscs.json source test",
    "precommit": "npm run check && npm run critique && npm run coverage",
    "precoverage": "istanbul cover --config .dotfiles/istanbul.yml -- _mocha --opts .dotfiles/mocha.opts",
    "test": "mocha --opts .dotfiles/mocha.opts"
  },
  "dependencies": {
    "commonform-predicate": "^0.4.0"
  }
}
